# ایجاد محیط
conda create -n geosphereai python=3.9 -y

# فعال‌سازی
conda activate geosphereai

# نصب کتابخانه‌ها
pip install flask werkzeug==2.3.7 pillow opencv-python numpy matplotlib tqdm tensorflow==2.13.0 transformers==4.34.0 torch torchvision

# تست
python -c "import flask, cv2, numpy, tensorflow, torch; print('همه چیز OK!')"


# 1. حذف PyTorch فعلی
pip uninstall torch torchvision torchaudio -y

# 2. نصب PyTorch با CUDA
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# 3. تست
python -c "import torch; print('CUDA Available:', torch.cuda.is_available())"